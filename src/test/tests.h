#ifndef TEST_TESTS_H
#define TEST_TESTS_H

/* HACK: Sync the length of this array def
   with the array of the same name in tests.c.
   This is also the only thing that should be
   here.
*/
extern test_t tests[1];

#endif /* TEST_TESTS_H */
